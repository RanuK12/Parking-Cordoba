Para ejecutar el script de detección de espacios de parking, utiliza el siguiente comando:

```bash
python detect_parking.py --input test_image.jpg --output output.mp4
```

El script procesará la imagen o video de entrada, distinguirá los espacios ocupados de los libres, y mostrará los resultados en una ventana o guardará la salida en un archivo.